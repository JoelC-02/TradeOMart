import { createTables, insertIntoTables, dropTables, createTables2, insertIntoTables2, dropTables2, createTables3, insertIntoTables3, dropTables3 } from './queryFunctions';

(async () => {
  await createTables3();
  // await insertIntoTables();
  // await dropTables();
})();